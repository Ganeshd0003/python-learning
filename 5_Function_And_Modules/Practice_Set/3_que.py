# Write a function full_name(first, last) that takes first name and last name as parameters and returns a single string in the format "Last First".

def full_name(first_name, last_name):
    return f"{last_name} {first_name}"

print(full_name("Ganesh","Dabahde"))